# Signal Scout Frontend

```bash
npm run dev    # dev server at localhost:3000
npm run build  # static export to out/
```

Set `NEXT_PUBLIC_API_BASE` to override the API base URL (defaults to same-origin).
